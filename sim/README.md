# Simulation Directory

This directory contains simulation scripts, test runners, and Makefiles for the Titan X5-B GPU.
All tests use **Cocotb** for native hardware verification via Python.

Run `make test` to execute the full cocotb test suite.
