# Synthesis Directory

This directory contains synthesis scripts and generated reports for the Titan X5-B GPU.
We primarily use **Yosys** to extract gate counts and evaluate synthesis viability.

Run the scripts in `scripts/` to generate reports in `reports/`.
